/* =============================================
   TOEFL Prep Hub – Ad Management
   Video ads removed (pending Google Ad Manager approval).
   Only AdSense display/banner ads are active.
   ============================================= */

// Push AdSense banner slots on load
document.addEventListener('DOMContentLoaded', function() {
  // AdSense auto-initialises via the global script tag in each HTML file.
  // Nothing extra needed here for display ads.
  console.log('[AdManager] AdSense banner slots ready.');
});